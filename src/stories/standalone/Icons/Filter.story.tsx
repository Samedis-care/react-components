import React from "react";
import { FilterIcon } from "../../../standalone";

export const FilterIconStory = (): React.ReactElement => {
	return <FilterIcon />;
};

FilterIconStory.storyName = "Filter";
