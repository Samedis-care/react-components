import { IconButton, styled } from "@mui/material";

export const SmallIconButton = styled(IconButton)({
	padding: 4,
}) as typeof IconButton;

export const SmallestIconButton = styled(IconButton)({
	padding: 0,
}) as typeof IconButton;
